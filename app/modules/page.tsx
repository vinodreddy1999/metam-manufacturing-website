import Link from "next/link"; import {modules} from "../data/modules";
export const metadata={title:"Modules"};
export default function Modules(){return <main><section className="page-hero compact shell"><span className="eyebrow"><i/> Modules</span><h1>Build your platform <em>capability by capability.</em></h1><p className="lede">Every module works independently. Together, they create one connected operational model.</p></section><section className="section shell module-catalog">{modules.map((m,i)=><Link href={`/modules/${m.slug}`} className="catalog-card" key={m.slug}><div><span className="module-index">{String(i+1).padStart(2,"0")}</span><div className="module-icon">{m.icon}</div></div><div><h2>{m.name}</h2><p>{m.description}</p><ul>{m.outcomes.map(o=><li key={o}>{o}</li>)}</ul><span className="text-link">View module →</span></div></Link>)}</section></main>}
