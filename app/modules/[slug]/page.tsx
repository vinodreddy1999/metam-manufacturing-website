import Link from "next/link"; import {notFound} from "next/navigation"; import {modules} from "../../data/modules";
export function generateStaticParams(){return modules.map(m=>({slug:m.slug}))}
export default async function ModuleDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const m=modules.find(x=>x.slug===slug);if(!m)notFound();return <main><section className="page-hero module-hero shell"><div><span className="eyebrow"><i/> METAM module</span><div className="large-module-icon">{m.icon}</div><h1>{m.name}</h1><p className="lede">{m.description}</p><div className="actions"><Link className="button primary" href="/pricing">Add to your platform <span>↗</span></Link><Link className="button ghost dark" href="/modules">All modules</Link></div></div><div className="outcome-panel"><span>Operational outcomes</span>{m.outcomes.map((o,i)=><div key={o}><b>0{i+1}</b><p>{o}</p></div>)}</div></section><section className="section shell"><div className="section-heading"><div><span className="kicker">Core capabilities</span><h2>Everything teams need to move the work.</h2></div><p>Configured to your processes, roles, plants, and governance model.</p></div><div className="capability-grid">{m.capabilities.map((c,i)=><article key={c}><span>{String(i+1).padStart(2,"0")}</span><h3>{c}</h3><p>Connected records, role-aware actions, validation, exceptions, and audit context built in.</p></article>)}</div></section><section className="connect-band"><div className="shell"><span className="kicker">Connected by default</span><h2>{m.name} works better with</h2><div>{m.connects.map(c=><span key={c}>{c}</span>)}</div></div></section></main>}
